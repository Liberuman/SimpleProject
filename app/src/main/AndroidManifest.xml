<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sxu.simpleproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 微信支付 -->
        <!-- <activity -->
        <!-- android:name=".wxapi.WXEntryActivity" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:exported="true" -->
        <!-- android:launchMode="singleTop"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->


        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.VIEW"/> -->
        <!-- <category android:name="android.intent.category.DEFAULT"/> -->
        <!-- <data android:scheme="sdksample"/> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity android:name=".BaseHomeActivity">
        </activity>
        <activity android:name=".BaseActivityStyleActivity">
        </activity>
        <activity android:name=".BaseProgressActivityStyleActivity">
        </activity>
        <activity android:name=".DialogHomeActivity">
        </activity>
        <activity android:name=".NavigationBarHomeActivity">
        </activity>
    </application>

</manifest>